# The remove keyword uses an argument of the ITEM or ELEMENT that you wish to remove
# It also only removes the FIRST occurance of that element
# Example:
colors = ["Red", "Blue", "Green", "Purple"]
colors.remove("Blue")
print(colors)